// Richiesta al cliente il numero di kilometri che vuole percorre
const stringkil = prompt("Inserire i kilometri che si vogliono percorre:");//string|null
const kil = parseInt(stringkil);    // conversione da stringa ad un numero intero 
console.log(kil);   //string|null
// Richiesta al cliente la propria età
const stringeta = prompt("Inserire l'età:");
const eta = parseInt(stringeta);    // conversione da stringa ad un numero intero 
console.log(eta);   //string|null
